VITE_SUPABASE_URL=https://qslksdgxoibzrisywvqk.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_HqXFypbh0cTO8Eub41LlQw_8ypkj2tH

# Which sign-in buttons to show above the emailed code: apple, google, or
# both. Empty — the default — shows only the code, which is the right way to
# fail: a button for a provider the Supabase project has not been told about
# answers "Unsupported provider", and nobody should be shown that.
#
# Set this only once the provider is enabled in Supabase (Auth → Sign In /
# Providers) with its keys from the Apple Developer and Google Cloud
# consoles. See docs/ways-in.md.
VITE_WAYS_IN=
